export type Plant = {
    id: number,
    name: string,
    species: string,
    imageUrl: string
}

export type Job = {
    id: number,
    frequency: string,
    jobCreationDate: string,
    lastRunTime: string
}